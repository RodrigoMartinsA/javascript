let valores = [4, 5, 6, 7, 8 ]

for (let pos = 0; pos < valores.length; pos++) {
    console.log(`A posição ${pos} tem o valor ${valores[pos]} `)
}

let num = [1, 2, 3, 4, 5, 6]
for (pre in num) {
    console.log(`A posição ${pre} tem o valor ${num[pre]}`)
}

